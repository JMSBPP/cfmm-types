# types.toml — Numerics domain (tracked under .spec/types/)
# Pointer on modules: -- types.toml: Ray

[Ray]
module = "Ray"
kind = "plain"
file = "types/Ray/Ray.md"
carriers = ["Ray"]
operations = ["intro", "rayVal", "rayMulId", "rayAddId", "rayMax"]
laws = [
  "rayVal(intro(x)) = x",
  "rayMulId() = intro(RAY_UNIT)",
  "rayAddId() = intro(RAY_ZERO)",
  "rayMax() = intro(floor(U256_MAX / RAY_UNIT))",
  "RAY_UNIT = 10^27",
]
refined = false
notes = "Domain Numerics (#28). Migrate draft RayAlgebra carrier+ids only; Expansion/Contraction/scaleRay deferred. Independent of TimeSpacing sqrt_dt (#26). No Eff. No Weiner bodies."
